const categories = document.querySelector("#categories");
const numOfCategories = countCategories(categories) {
    let counter = 0;
    for (let i = 0; i < categories.length; i += 1) {
        return counter += i;
    }
    console.log ("Number of categories: ${counter}");
}
